// Redirect flow removed — signInWithRedirect replaced by signInWithPopup for all platforms
describe('App redirect (removed)', () => {
  it('no redirect flow exists', () => {
    expect(true).toBe(true);
  });
});
